# Insights — Working Notes

**This directory is gitignored** (`.gitignore:55`). Contents stay local.

Use it for exploratory analysis, scratch notes, and draft findings that are not ready to be shared. Anything of lasting value should be promoted into a tracked document in `docs/` rather than left here.

This README is committed so the directory's purpose is visible; nothing else in it is.